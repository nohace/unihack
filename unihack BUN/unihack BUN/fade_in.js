document.addEventListener("DOMContentLoaded", function () {
    const logo = document.getElementById("logo");
    logo.style.transition = "opacity 1s ease"; // Add a smooth transition effect to opacity
    logo.style.opacity = 1; // Change opacity to 1 to make it visible (fade-in effect)

    const text = document.getElementById("text");
    text.style.transition = "opacity 1s ease"; // Add a smooth transition effect to opacity
    text.style.opacity = 1; // Change opacity to 1 to make it visible (fade-in effect)

    document.addEventListener("DOMContentLoaded", function () {
        const box = document.getElementById("box");
        box.style.opacity = 1; // Change opacity to 1 to make the section visible (fade-in effect)
    });
    
});
